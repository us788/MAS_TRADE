# CLAUDE.md

이 저장소에서 작업하는 AI 어시스턴트가 따라야 할 규칙.

---

## 1. 비밀 정보 — 예외 없음

**`.env` 파일을 읽지 않는다.**

- `.env`, `.env.local`, `.env.production` 등 모든 `.env*` 파일(단 `.env.example` 제외)은
  열람·출력·복사·요약 대상이 아니다. `cat`, `read`, `grep`, `head`, 에디터 열람 전부 해당한다.
- 키 값이 필요해 보이면 **파일을 읽지 말고 사용자에게 물어본다.**
- 코드에서는 `os.getenv("KEY_NAME")`으로 참조만 한다. 값을 로그·주석·커밋·응답에 남기지 않는다.
- 실수로 키 값이 눈에 들어왔다면 **응답에 옮겨 적지 않고 사용자에게 노출 사실만 알린다.**
- `.gitignore`에서 `.env` 항목을 제거하거나 완화하지 않는다.

새 키가 필요하면 `.env.example`에 **이름과 주석만** 추가한다. 값은 비워 둔다.

## 2. 커밋

- **커밋 메시지에 Claude 공동저자를 넣지 않는다.** `Co-Authored-By: Claude` 트레일러,
  "Generated with Claude Code" 문구, Claude 세션 링크 전부 금지. 저작자는 ys 본인만이다.
- 커밋 전에 `git status`로 `.env`나 `data/`, `logs/`가 스테이징되지 않았는지 확인한다.
- 파일을 생성하거나 수정하기 전에 사용자에게 확인받는다.

## 3. 이 프로젝트의 설계 규칙

어시스턴트가 코드를 쓸 때 지켜야 할 것들. 근거는 `docs/plan.md`에 있다.

### 계산은 코드, 해석은 LLM

PER·변동성·상관계수·베타·샤프 등 **모든 수치 계산은 `src/compute/`의 결정론적 코드**가 한다.
LLM 프롬프트 안에서 산술을 시키지 않는다. LLM이 만든 틀린 숫자는 근거로 로그에 남아
사후 검증까지 오염시킨다. LLM의 역할은 주어진 수치의 **해석·비교·판단**뿐이다.

### 시점 정합성

미래 정보가 새어 들어가는 코드를 쓰지 않는다.

- 매크로 지표는 **ALFRED vintage**로 그 시점 발표치를 쓴다. 수정 최종치를 쓰면 룩어헤드다.
- 재무 데이터는 회계기간 종료일이 아니라 **공시 제출일**(EDGAR filing date / DART 접수일자) 기준.
- 날짜가 아니라 **타임스탬프**로 조인한다. CPI는 장전 08:30, FOMC는 장중 14:00이다.
- 뉴스는 발행 시각 기준으로 필터한다.

데이터 접근은 반드시 as-of 인터페이스를 통한다. 원본 소스를 직접 조회하는 코드를 에이전트에 넣지 않는다.

### 리스크 엔진은 거부권을 가진다

리스크 레이어는 조언자가 아니라 승인/기각 게이트다. 기각 사유(섹터 집중, 상관 과다,
변동성 레짐, 포지션 한도)는 코드로 판정하고 기록한다.

### 전량 로깅

프롬프트, 응답, 사용한 데이터 스냅샷, 기각 사유를 모두 남긴다. 재현성의 전제이자,
에이전트 부분집합 사후 재채점(무료 ablation)의 기반이다.

저장 위치는 두 곳으로 나뉜다.

- **`data/mas_trade.sqlite3`** — 기사·수집이력·gap. 중복 제거는 `(symbol, content_hash)`
  PRIMARY KEY가 구조적으로 보장한다. 재수집 시 `INSERT OR IGNORE`이므로
  **`collected_at`은 최초 관측 시각으로 고정된다** — known_at의 보수적 판정이 이 값에
  기대므로 나중 값으로 덮지 않는다.
- **`data/snapshots/raw/`** — 벤더 원본 응답. 파서를 고쳤을 때 재현하려면 필요하다.
  거의 읽지 않으므로 DB에 넣지 않는다.

as-of 조회는 **항상 `known_at` 기준**이다. `published_at`으로 거르면 벤더가 준 시각이
깨졌을 때 룩어헤드가 샌다.

### 시장 분기

US/KR 어댑터는 공통 인터페이스 뒤에 숨긴다. 상위 에이전트 코드는 시장을 몰라야 한다.
**단, 프롬프트는 시장별로 따로 관리한다.** 미국장용을 번역만 해서 한국장에 쓰지 않는다.
공시 형식, 회계 표기, 뉴스 톤, 개인 수급 비중, 공매도 규제가 전부 다르다.

### LLM 호출은 `src/llm/`만 통한다

활용 LLM은 **DeepSeek 확정**이다. 에이전트 코드에서 벤더 SDK를 직접 부르지 않는다.

- OpenAI ChatCompletions 호환이므로 **base_url·모델명 교체만으로 벤더를 바꿀 수 있는 상태**를 유지한다.
  모델명을 코드에 하드코딩하지 않는다. `src/config.py`의 `LLM_MODEL_FAST` / `LLM_MODEL_DEEP`를 쓴다.
- **모델 버전을 로그에 남긴다.** DeepSeek는 같은 별칭 뒤에서 모델을 갱신한다. 응답이 보고한
  버전 문자열·호출 시각·토큰·캐시 적중·추정 비용을 호출마다 기록한다. 이게 없으면 그 결과는
  재현 불가로 취급한다.
- **프리픽스 캐싱을 깨지 않는다.** 캐시 히트 단가가 미스보다 모델에 따라 30~50배 싸다. 시스템 프롬프트·용어
  정의·스키마 설명은 **바이트 단위로 동일한 고정 프리픽스**로 두고, 종목별 가변부를 뒤에 붙인다.
  날짜·종목명·랜덤 ID를 프리픽스에 넣지 않는다.
- `temperature`는 0이 기본이다. 올리는 코드를 쓰려면 이유를 남긴다.
- 1M 컨텍스트를 이유로 원문을 통째로 밀어넣지 않는다. 에이전트 간 전달은 구조화 JSON이다.
- **프롬프트에 넣지 않는 것**: 계좌 정보, 개인 식별 정보, 타 서비스 키. 공개 시장 데이터와
  공시 원문만 보낸다.

### 뉴스 수집

소스는 시장별로 다르다. US는 Finnhub `company-news`, KR은 네이버 검색 API가 메인이고,
Alpha Vantage는 **센티먼트 베이스라인 전용**이다(일 25회라 수집기로 쓰지 않는다).
소스별 한도·제약은 `docs/data-sources.md`.

- **`published_at`과 `collected_at`을 둘 다 저장한다.** 벤더가 주는 발행 시각에는 재발행·수정
  시각이 섞여 들어온다. 둘이 어긋나거나 발행 시각이 수상하면 보수적으로 `collected_at`을 쓴다.
- **Alpha Vantage 펀더멘털(`OVERVIEW`, `INCOME_STATEMENT`, `EARNINGS`)을 쓰지 않는다.**
  현재 스냅샷만 주기 때문에 과거 시점 값을 되돌려 받을 수 없다. 재무는 EDGAR filing date /
  DART 접수일자 기준을 유지한다.
- 네이버 API 응답은 그대로 쓰지 않는다. `pubDate`는 KST이므로 UTC로 정규화하고,
  `title`·`description`의 `<b>` 태그와 HTML 엔티티를 제거한 뒤 프롬프트에 넣는다.
- **기사 본문을 크롤링하지 않는다.** API가 주는 제목·요약·링크·발행시각까지만 저장한다.
  무료 티어는 대부분 재배포 금지이므로, 로그를 공개할 때는 본문을 빼고 URL·발행시각·해시만 남긴다.
- 수집 실패를 예외가 아니라 정상 경로로 다룬다. 실패한 날짜를 기록하고 재시도한다.
  과거 뉴스는 나중에 살 수 없어 **거른 날은 영구 손실**이다.

## 4. 하지 않을 것

- **실주문 경로를 만들지 않는다.** 증권사 주문 API 연동 코드를 작성하지 않는다.
  `RUN_MODE`는 `paper` 외의 값을 지원하지 않는다.
- **수익률을 통화 환산해 합산하지 않는다.** US와 KR은 각자 현지통화로 끝까지 따로 본다.
- **백테스트 성적을 실전 기대수익으로 서술하지 않는다.** LLM은 과거 구간 결과를 이미 알고 있어
  과거 백테스트는 구조적으로 과대평가된다. DeepSeek는 학습 컷오프를 공시하지 않으므로
  V4 프리뷰 공개일(2026-04-24) 이후를 보수적 대리 기준으로 쓴다.
- **`data/`와 `logs/`를 커밋하지 않는다.**

## 5. 작업 일지

설계 결정을 바꾸거나, 원인을 모른 채 막히거나, 벤더 제약을 새로 알아냈으면
`docs/journal/YYYY-MM-DD.md`에 남긴다. 형식과 규칙은 `docs/journal/README.md`.

- **기각한 선택지와 그 이유**를 함께 적는다. 근거가 없으면 반년 뒤에 같은 검토를 반복한다.
- **미해결 문제도 남긴다.** 증상, 확인한 사실, 가설, 아직 확인하지 못한 것을 구분해 적는다.
- **틀린 판단은 지우지 않고 정정으로 남긴다.**
- 기획서(`docs/plan.md`)는 현재 상태를, 일지는 거기까지 온 경로를 담는다.
